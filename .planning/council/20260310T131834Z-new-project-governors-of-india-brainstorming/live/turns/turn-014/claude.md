Perfect—locking in 1947 to present makes sense. It's a bigger lift upfront, but the constitutional anchoring and source verification rules we've baked in will scale through the scraping rounds. Each pass fills gaps, and we iterate on the query strategy as we hit coverage limits. By the time Phase 0 closes, we've got watertight data; Phase 1 tunes the beard scale bands to reality. The schema stays solid through all of it.

I've written the full brainstorming doc and saved it to BRAINSTORM.md in the project root—it walks through the schema, the severity math, the five-phase roadmap, and the data collection strategy (hand-curated archives + court judgments + RTI + crowdsourced flags, all schema-agnostic and moderation-gated). The doc nails down what Phase 0 owns: schema lock, collection pipeline, source verification. Everything downstream (cockpit, drawers, QA) hangs on that foundation.

One thread I want to circle back on once we're deep in Phase 0: as we scrape historical incidents, some governors will be dead or out of office for decades. Should we surface a "historical context" lens—like "this was a 1975 crisis under Emergency"—or keep the timeline view governor-agnostic and let the data speak? It doesn't change the schema, but it shapes how users navigate the evidence. Just wanted to flag it early.
